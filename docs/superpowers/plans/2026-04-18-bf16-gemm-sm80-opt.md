# BF16 GEMM SM80 Optimized (Swizzle + LDSM) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create `bf16_gemm_sm80_opt.cu` — a heavily-commented BF16 GEMM kernel with swizzled smem and LDSM smem→regs copy, evolved from the plain `bf16_gemm_sm80.cu`.

**Architecture:** Copy the existing sample 1 file, then modify three areas: (1) smem layouts from plain to swizzled via `Swizzle<3,3,3>` composition + `tile_to_shape`, (2) add LDSM s2r copy via `make_tiled_copy_A/B`, `partition_S`, `retile_D`, (3) update the main loop to use the LDSM copy path. Everything else (MMA, CTA tile, gmem→smem, epilogue, host main) stays the same.

**Tech Stack:** CUDA/CuTe, SM80 tensor cores (`SM80_16x8x16_F32BF16BF16F32_TN`), SM75 LDSM (`SM75_U32x4_LDSM_N`), CUTLASS headers.

---

### Task 1: Create the file with updated file header and includes

**Files:**
- Create: `cute-reference/samples/bf16_gemm_sm80_opt.cu`

- [ ] **Step 1: Copy sample 1 as the starting point**

Run:
```bash
cp cute-reference/samples/bf16_gemm_sm80.cu cute-reference/samples/bf16_gemm_sm80_opt.cu
```

- [ ] **Step 2: Update the file header comment**

Replace lines 1-16 of `bf16_gemm_sm80_opt.cu` with:

```cpp
/***************************************************************************************************
 * BF16 GEMM using SM80 tensor cores with CuTe — Optimized Sample
 *
 * Demonstrates (on top of the basic sample):
 *   - Swizzled shared memory layouts to avoid bank conflicts
 *   - LDSM (ldmatrix) vectorized smem→register copy
 *   - make_tiled_copy_A/B for deriving s2r copy from TiledMMA
 *   - retile_D for bridging LDSM register layout and MMA register layout
 *
 * C = alpha * A * B^T + beta * C
 *   A: bf16, M x K, row-major (TN layout)
 *   B: bf16, K x N, stored as (N, K) in CuTe with K-contiguous stride
 *   C: f32, M x N, column-major
 *
 * Target: SM80+ tensor cores (runs on SM90 L20Y GPU)
 *
 * Compared to bf16_gemm_sm80.cu (plain sample):
 *   - Smem layouts: plain → swizzled (Swizzle<3,3,3>)
 *   - smem→regs copy: scalar → LDSM (SM75_U32x4_LDSM_N)
 *   - Everything else unchanged (MMA atom, CTA tile, gmem→smem, epilogue)
 **************************************************************************************************/
```

- [ ] **Step 3: Commit**

```bash
git add cute-reference/samples/bf16_gemm_sm80_opt.cu
git commit -m "Create bf16_gemm_sm80_opt.cu from plain sample with updated header"
```

---

### Task 2: Modify the host function — swizzled smem layouts and s2r atoms

**Files:**
- Modify: `cute-reference/samples/bf16_gemm_sm80_opt.cu` (host function `bf16_gemm_tn`, lines ~214-278)

- [ ] **Step 1: Update the host function comment block**

Replace the host function section comment (lines ~201-212) with:

```cpp
// ================================================================================================
// Host Function — configure and launch the kernel
// ================================================================================================
//
// This function sets up all the static (compile-time) parameters:
//   - Problem shape and strides (dynamic, from arguments)
//   - CTA tile sizes (static: 128 x 128 x 32)
//   - Smem layouts (static, swizzled with Swizzle<3,3,3>)
//   - Thread layouts for copy (static)
//   - TiledMMA (static, wraps SM80 BF16 tensor core atom)
//   - S2R copy atoms (static, SM75_U32x4_LDSM_N)
//
// Then computes grid dimensions and launches the device kernel.
```

- [ ] **Step 2: Add s2r atom template parameters to the host function signature**

Replace the host function signature:

```cpp
template <class Alpha, class Beta>
void
bf16_gemm_tn(int m, int n, int k,
             Alpha alpha,
             bf16_t const* A, int ldA,
             bf16_t const* B, int ldB,
             Beta beta,
             float* C, int ldC,
             cudaStream_t stream = 0)
```

with:

```cpp
template <class S2RCopyAtomA, class S2RCopyAtomB, class Alpha, class Beta>
void
bf16_gemm_tn(int m, int n, int k,
             Alpha alpha,
             bf16_t const* A, int ldA,
             bf16_t const* B, int ldB,
             Beta beta,
             float* C, int ldC,
             cudaStream_t stream = 0)
```

- [ ] **Step 3: Replace the smem layout construction**

Replace these lines (the plain smem layout construction):

```cpp
  // Smem layouts (static, plain — no swizzle)
  // make_layout(shape) produces a layout with default contiguous strides.
  // (128, 32) -> stride (32, 1) — K-contiguous, efficient for row-major loading
  auto sA = make_layout(make_shape(bM, bK));                            // (128, 32) stride (32, 1)
  auto sB = make_layout(make_shape(bN, bK));                            // (128, 32) stride (32, 1)
  auto sC = make_layout(make_shape(bM, bN));                            // (128, 128) — unused in kernel
```

with:

```cpp
  // Smem layouts (static, swizzled)
  //
  // XOR swizzle eliminates shared memory bank conflicts.
  // When multiple threads in a warp access different rows of smem in the same
  // cycle, they can collide on the same 32-bit bank. Swizzle XORs address bits
  // so that logically-adjacent rows map to different banks.
  //
  // Swizzle<3,3,3> means: 3-bit XOR, base position 3, shift 3.
  //   XORs bit group [5:3] with bit group [8:6] in the byte address.
  //   This is a 128-byte swizzle — the standard pattern for 16-bit element GMMA/LDSM.
  //
  // The base layout is 8 x (8 x 8) with strides (8, (1, 64)):
  //   - Outer dimension: 8 rows of swizzle tiles
  //   - Inner (8, 8): 8 rows x 8 columns per tile, column-major within each tile
  //   - Stride (1, 64): column-major inner, 64-element gap between inner tile groups
  //
  // tile_to_shape replicates this swizzle atom to cover the full smem shape.
  // The resulting ComposedLayout applies: layout(coord) → offset, then XOR-swizzle(offset).

  auto swizzle_atom = composition(
      Swizzle<3, 3, 3>{},
      Layout<Shape <_8, Shape<_8, _8>>,
             Stride<_8, Stride<_1, _64>>>{});

  auto sA = tile_to_shape(swizzle_atom, make_shape(bM, bK));            // (128, 32) swizzled
  auto sB = tile_to_shape(swizzle_atom, make_shape(bN, bK));            // (128, 32) swizzled
  auto sC = make_layout(make_shape(bM, bN));                            // (128, 128) — unused in kernel
```

- [ ] **Step 4: Add s2r copy atom definitions after TiledMMA**

After the TiledMMA block and the `static_assert`, add:

```cpp

  // S2R (smem→register) copy atoms (static)
  //
  // SM75_U32x4_LDSM_N wraps the ldmatrix.sync.aligned.x4.m8n8.shared.b16 PTX instruction.
  // One warp (32 threads) cooperatively loads a 8x8 matrix of 16-bit values (128 bytes)
  // from smem into registers. Each thread receives 4 x 32-bit = 128 bits.
  //
  // Works for BF16 because the instruction operates on .b16 (16-bit) without
  // interpreting the data — it just moves bits.
  //
  // _N suffix: normal (non-transposing) layout. Our smem is K-contiguous, and the
  // MMA atom (SM80_16x8x16_F32BF16BF16F32_TN) expects TN layout, so no transpose needed.
  Copy_Atom<SM75_U32x4_LDSM_N, bf16_t> s2r_atom_a;
  Copy_Atom<SM75_U32x4_LDSM_N, bf16_t> s2r_atom_b;
```

- [ ] **Step 5: Update the kernel launch call to pass s2r atoms**

Replace:

```cpp
  // Launch kernel
  bf16_gemm_device<<<dimGrid, dimBlock, 0, stream>>>(
      prob_shape, cta_tiler,
      A, dA, sA, tA,
      B, dB, sB, tB,
      C, dC, sC, mma,
      alpha, beta);
```

with:

```cpp
  // Launch kernel
  bf16_gemm_device<<<dimGrid, dimBlock, 0, stream>>>(
      prob_shape, cta_tiler,
      A, dA, sA, tA,
      B, dB, sB, tB,
      C, dC, sC, mma,
      s2r_atom_a, s2r_atom_b,
      alpha, beta);
```

- [ ] **Step 6: Commit**

```bash
git add cute-reference/samples/bf16_gemm_sm80_opt.cu
git commit -m "Update host function: swizzled smem layouts and LDSM s2r atoms"
```

---

### Task 3: Modify the device kernel — s2r setup and main loop

**Files:**
- Modify: `cute-reference/samples/bf16_gemm_sm80_opt.cu` (device kernel, lines ~45-199)

- [ ] **Step 1: Update the kernel template and signature to accept s2r atoms**

Replace the kernel template and signature (lines 45-57):

```cpp
template <class ProblemShape, class CtaTiler,
          class TA, class AStride, class ASmemLayout, class AThreadLayout,
          class TB, class BStride, class BSmemLayout, class BThreadLayout,
          class TC, class CStride, class CSmemLayout, class TiledMma,
          class Alpha, class Beta>
__global__ static
__launch_bounds__(decltype(size(TiledMma{}))::value)
void
bf16_gemm_device(ProblemShape shape_MNK, CtaTiler cta_tiler,
                 TA const* A, AStride dA, ASmemLayout sA_layout, AThreadLayout tA,
                 TB const* B, BStride dB, BSmemLayout sB_layout, BThreadLayout tB,
                 TC      * C, CStride dC, CSmemLayout          , TiledMma mma,
                 Alpha alpha, Beta beta)
```

with:

```cpp
template <class ProblemShape, class CtaTiler,
          class TA, class AStride, class ASmemLayout, class AThreadLayout,
          class TB, class BStride, class BSmemLayout, class BThreadLayout,
          class TC, class CStride, class CSmemLayout, class TiledMma,
          class S2RCopyAtomA, class S2RCopyAtomB,
          class Alpha, class Beta>
__global__ static
__launch_bounds__(decltype(size(TiledMma{}))::value)
void
bf16_gemm_device(ProblemShape shape_MNK, CtaTiler cta_tiler,
                 TA const* A, AStride dA, ASmemLayout sA_layout, AThreadLayout tA,
                 TB const* B, BStride dB, BSmemLayout sB_layout, BThreadLayout tB,
                 TC      * C, CStride dC, CSmemLayout          , TiledMma mma,
                 S2RCopyAtomA s2r_atom_a, S2RCopyAtomB s2r_atom_b,
                 Alpha alpha, Beta beta)
```

- [ ] **Step 2: Update the device kernel section comment at top of function**

Replace the data flow comment block (lines 29-43):

```cpp
// ================================================================================================
// Device Kernel
// ================================================================================================
//
// Each thread block computes a (BLK_M x BLK_N) tile of output C.
// The K dimension is processed in chunks of BLK_K.
//
// Data flow (per K-tile):
//   1. gmem A,B --> shared memory  (all threads cooperate via thread layout)
//   2. __syncthreads()
//   3. smem A,B --> registers      (MMA-partitioned copy)
//   4. tensor core MMA             (gemm on register fragments)
//   5. __syncthreads()
//
// After all K-tiles: accumulators --> gmem C (epilogue)
```

with:

```cpp
// ================================================================================================
// Device Kernel
// ================================================================================================
//
// Each thread block computes a (BLK_M x BLK_N) tile of output C.
// The K dimension is processed in chunks of BLK_K.
//
// Data flow (per K-tile):
//   1. gmem A,B --> shared memory  (all threads cooperate via thread layout)
//      Smem is swizzled — bank-conflict-free for the LDSM reads that follow.
//   2. __syncthreads()
//   3. smem A,B --> registers      (LDSM vectorized copy via make_tiled_copy_A/B)
//   4. tensor core MMA             (gemm on register fragments)
//   5. __syncthreads()
//
// After all K-tiles: accumulators --> gmem C (epilogue)
//
// Optimizations over bf16_gemm_sm80.cu:
//   - Swizzled smem layouts: XOR address bits to avoid bank conflicts
//   - LDSM smem→regs: vectorized ldmatrix instruction instead of scalar loads
```

- [ ] **Step 3: Update the smem section comment**

Replace the Step 2 comment (lines 97-100):

```cpp
  // ---- Step 2: Shared memory tensors ----
  //
  // Static __shared__ arrays sized by cosize_v (number of elements the layout addresses).
  // Plain layouts — no swizzle, no padding. K-contiguous for efficient gmem loading.
```

with:

```cpp
  // ---- Step 2: Shared memory tensors ----
  //
  // Static __shared__ arrays sized by cosize_v (number of elements the layout addresses).
  // Swizzled layouts — XOR address manipulation eliminates bank conflicts.
  // The swizzle is transparent to all copy operations (gmem→smem writes just work).
  // cosize_v accounts for any padding introduced by the swizzle pattern.
```

- [ ] **Step 4: Add the s2r setup after register fragment allocation**

After the register fragment allocation block (after `Tensor tCrC = ...;` and before `// Zero the accumulators`), add the s2r TiledCopy setup. Replace the comment block at lines 140-157 (the `make_fragment_A/B` explanation) with an updated version, and add the s2r setup after it.

Replace lines 140-163:

```cpp
  // Allocate register fragments.
  //
  // make_fragment_A/B creates a register tensor with the same logical shape as
  // the smem partition (tCsA/tCsB), but with a register layout that matches what
  // the MMA hardware instruction expects. This is the key correctness mechanism:
  //
  //   partition_A gives a smem layout — where data sits in shared memory
  //   make_fragment_A gives a register layout — which register holds which (m,k) value
  //
  // Both share the same logical coordinate space (MMA, MMA_M, MMA_K), so
  // copy(tCsA, tCrA) maps each logical element to the correct register.
  // The copy iterates over logical coordinates, not physical addresses —
  // it reads tCsA(v,m,k) from smem and writes to tCrA(v,m,k) in the register
  // that the MMA expects for that (m,k) position.
  //
  // Tradeoff: without a dedicated smem→regs copy atom (e.g. SM75_U32x4_LDSM_N),
  // the copy generates scalar loads. Correct but slow. The production path uses
  // make_tiled_copy_A(s2r_atom, mma) to vectorize this step.
  Tensor tCrA = thr_mma.make_fragment_A(tCsA);                         // (MMA, MMA_M, MMA_K) in regs
  Tensor tCrB = thr_mma.make_fragment_B(tCsB);                         // (MMA, MMA_N, MMA_K) in regs
  Tensor tCrC = thr_mma.make_fragment_C(tCgC);                         // (MMA, MMA_M, MMA_N) accum

  // Zero the accumulators
  clear(tCrC);
```

with:

```cpp
  // Allocate register fragments.
  //
  // make_fragment_A/B creates a register tensor with the same logical shape as
  // the smem partition (tCsA/tCsB), but with a register layout that matches what
  // the MMA hardware instruction expects.
  //
  //   partition_A gives a smem layout — where data sits in shared memory
  //   make_fragment_A gives a register layout — which register holds which (m,k) value
  //
  // These register fragments (tCrA, tCrB) are the destination for the LDSM copy.
  // They are retiled via retile_D to match the LDSM instruction's output layout.
  Tensor tCrA = thr_mma.make_fragment_A(tCsA);                         // (MMA, MMA_M, MMA_K) in regs
  Tensor tCrB = thr_mma.make_fragment_B(tCsB);                         // (MMA, MMA_N, MMA_K) in regs
  Tensor tCrC = thr_mma.make_fragment_C(tCgC);                         // (MMA, MMA_M, MMA_N) accum

  // ---- Step 4b: S2R (smem→register) copy setup ----
  //
  // In the plain sample, smem→regs used scalar copy(tCsA, tCrA) — correct but slow.
  // Here we use LDSM (ldmatrix) for vectorized smem→register loads.
  //
  // make_tiled_copy_A(s2r_atom, mma) is the key bridge:
  //   - Takes an LDSM copy atom (the hardware instruction)
  //   - Takes the TiledMMA (which knows the thread-value layout)
  //   - Produces a TiledCopy whose thread mapping matches the MMA's expectations
  //   - No manual thread/value layout specification needed
  //
  // partition_S(sA) gives each thread's smem view for the LDSM load.
  // retile_D(tCrA) reshapes the MMA register fragment to match the LDSM
  //   destination layout. The MMA and LDSM may organize the same registers
  //   differently — retile handles this at the layout level (no data movement).
  //
  // In the main loop we pass the raw s2r_atom to copy(), not the TiledCopy,
  // because tiling/retiling has already been done by partition_S and retile_D.

  TiledCopy s2r_copy_a = make_tiled_copy_A(s2r_atom_a, mma);
  ThrCopy  thr_s2r_a   = s2r_copy_a.get_slice(threadIdx.x);
  Tensor tXsA = thr_s2r_a.partition_S(sA);                              // (CPY, MMA_M, MMA_K)
  Tensor tXrA = thr_s2r_a.retile_D(tCrA);                              // (CPY, MMA_M, MMA_K)

  TiledCopy s2r_copy_b = make_tiled_copy_B(s2r_atom_b, mma);
  ThrCopy  thr_s2r_b   = s2r_copy_b.get_slice(threadIdx.x);
  Tensor tXsB = thr_s2r_b.partition_S(sB);                              // (CPY, MMA_N, MMA_K)
  Tensor tXrB = thr_s2r_b.retile_D(tCrB);                              // (CPY, MMA_N, MMA_K)

  // Zero the accumulators
  clear(tCrC);
```

Note: The `tCsA/tCsB` (MMA smem partitions) are still computed by `thr_mma.partition_A/B` — they remain in the code unchanged. They're just no longer used for the smem→regs copy. The LDSM partitions (`tXsA/tXsB`) replace them for that purpose.

- [ ] **Step 5: Update the main loop to use LDSM copy**

Replace the main loop (lines 165-191):

```cpp
  // ---- Step 5: Main loop ----
  //
  // Simple non-pipelined loop over K-tiles:
  //   1. All threads copy gmem -> smem (via thread layout partitioning)
  //   2. __syncthreads() — wait for smem writes to complete
  //   3. Each thread copies its MMA-partitioned smem view -> registers
  //   4. Each thread issues tensor core gemm on registers
  //   5. __syncthreads() — wait for smem reads to complete (safe to overwrite next tile)

  auto K_TILE_MAX = size<2>(tAgA);

  for (int k_tile = 0; k_tile < K_TILE_MAX; ++k_tile)
  {
    // gmem -> smem
    copy(tAgA(_,_,k_tile), tAsA);
    copy(tBgB(_,_,k_tile), tBsB);
    __syncthreads();

    // smem -> registers (see make_fragment_A/B comment above for why this works)
    copy(tCsA, tCrA);
    copy(tCsB, tCrB);

    // Tensor core MMA: tCrC += tCrA * tCrB
    gemm(mma, tCrA, tCrB, tCrC);

    __syncthreads();
  }
```

with:

```cpp
  // ---- Step 5: Main loop ----
  //
  // Simple non-pipelined loop over K-tiles:
  //   1. All threads copy gmem -> swizzled smem (via thread layout partitioning)
  //      The swizzle is transparent — copy writes to logical addresses, CuTe applies XOR.
  //   2. __syncthreads() — wait for smem writes to complete
  //   3. Each thread loads from smem -> registers via LDSM (vectorized)
  //   4. Each thread issues tensor core gemm on registers
  //   5. __syncthreads() — wait for smem reads to complete (safe to overwrite next tile)
  //
  // The swizzled layout ensures warp-wide LDSM reads hit different banks,
  // eliminating the bank conflicts that would occur with a plain layout.

  auto K_TILE_MAX = size<2>(tAgA);

  for (int k_tile = 0; k_tile < K_TILE_MAX; ++k_tile)
  {
    // gmem -> smem (swizzle is transparent to the copy)
    copy(tAgA(_,_,k_tile), tAsA);
    copy(tBgB(_,_,k_tile), tBsB);
    __syncthreads();

    // smem -> registers via LDSM
    // copy(s2r_atom, src, dst) issues the ldmatrix instruction.
    // s2r_atom is the raw Copy_Atom, not the TiledCopy — tiling is already done.
    copy(s2r_atom_a, tXsA, tXrA);
    copy(s2r_atom_b, tXsB, tXrB);

    // Tensor core MMA: tCrC += tCrA * tCrB
    gemm(mma, tCrA, tCrB, tCrC);

    __syncthreads();
  }
```

- [ ] **Step 6: Commit**

```bash
git add cute-reference/samples/bf16_gemm_sm80_opt.cu
git commit -m "Add swizzled smem + LDSM to device kernel and main loop"
```

---

### Task 4: Update the main function to pass s2r atoms

**Files:**
- Modify: `cute-reference/samples/bf16_gemm_sm80_opt.cu` (main function)

- [ ] **Step 1: Update the kernel call in main**

The main function calls `bf16_gemm_tn`. Since the host function now requires explicit s2r atom template parameters, update the call. Replace:

```cpp
  // ---- Run kernel ----
  bf16_gemm_tn(m, n, k, alpha,
               d_A.data().get(), ldA,
               d_B.data().get(), ldB,
               beta,
               d_C.data().get(), ldC);
  CUTE_CHECK_LAST();
```

with:

```cpp
  // ---- Run kernel ----
  bf16_gemm_tn<Copy_Atom<SM75_U32x4_LDSM_N, bf16_t>,
               Copy_Atom<SM75_U32x4_LDSM_N, bf16_t>>(
      m, n, k, alpha,
      d_A.data().get(), ldA,
      d_B.data().get(), ldB,
      beta,
      d_C.data().get(), ldC);
  CUTE_CHECK_LAST();
```

Also update the benchmark loop call the same way. Replace:

```cpp
    bf16_gemm_tn(m, n, k, alpha,
                 d_A.data().get(), ldA,
                 d_B.data().get(), ldB,
                 beta,
                 d_C.data().get(), ldC);
```

with:

```bash
    bf16_gemm_tn<Copy_Atom<SM75_U32x4_LDSM_N, bf16_t>,
                 Copy_Atom<SM75_U32x4_LDSM_N, bf16_t>>(
        m, n, k, alpha,
        d_A.data().get(), ldA,
        d_B.data().get(), ldB,
        beta,
        d_C.data().get(), ldC);
```

Also update the printf in main to reflect the optimized version:

```cpp
  printf("BF16 GEMM (SM80 tensor cores): M=%d, N=%d, K=%d\n", m, n, k);
```

→

```cpp
  printf("BF16 GEMM (SM80 tensor cores, swizzle+LDSM): M=%d, N=%d, K=%d\n", m, n, k);
```

- [ ] **Step 2: Commit**

```bash
git add cute-reference/samples/bf16_gemm_sm80_opt.cu
git commit -m "Update main to pass LDSM s2r atoms to host function"
```

---

### Task 5: Update build_and_run.sh

**Files:**
- Modify: `cute-reference/samples/build_and_run.sh`

- [ ] **Step 1: Add compilation of the optimized sample**

Replace the contents of `cute-reference/samples/build_and_run.sh` with:

```bash
#!/bin/bash
set -e

CUTLASS_INC=/data/lmdeploy-cute/build/_deps/repo-cutlass-src/include

echo "Compiling bf16_gemm_sm80.cu (plain) ..."
nvcc -std=c++17 -arch=sm_90a \
     -I${CUTLASS_INC} \
     bf16_gemm_sm80.cu \
     -o bf16_gemm_sm80

echo "Compiling bf16_gemm_sm80_opt.cu (swizzle+LDSM) ..."
nvcc -std=c++17 -arch=sm_90a \
     -I${CUTLASS_INC} \
     bf16_gemm_sm80_opt.cu \
     -o bf16_gemm_sm80_opt

echo ""
echo "=== Running plain sample ==="
echo "--- 1024x1024x1024 ---"
./bf16_gemm_sm80 1024 1024 1024

echo ""
echo "=== Running optimized sample (swizzle+LDSM) ==="
echo "--- 1024x1024x1024 ---"
./bf16_gemm_sm80_opt 1024 1024 1024
```

- [ ] **Step 2: Commit**

```bash
git add cute-reference/samples/build_and_run.sh
git commit -m "Update build_and_run.sh to compile and run both samples"
```

---

### Task 6: Compile and verify correctness

**Files:**
- Test: `cute-reference/samples/bf16_gemm_sm80_opt.cu`

- [ ] **Step 1: Compile the optimized sample**

Run:
```bash
cd cute-reference/samples && nvcc -std=c++17 -arch=sm_90a -I/data/lmdeploy-cute/build/_deps/repo-cutlass-src/include bf16_gemm_sm80_opt.cu -o bf16_gemm_sm80_opt 2>&1
```

Expected: Clean compilation (no errors). IDE diagnostics from clang are false positives and can be ignored.

If compilation fails: read the error carefully, fix in the .cu file, and re-compile. Common issues:
- Missing `using namespace cute;` (should already be present from sample 1)
- Template parameter mismatch between host and device (check Task 1 and 3 changes)

- [ ] **Step 2: Run with default size and verify correctness**

Run:
```bash
cd cute-reference/samples && ./bf16_gemm_sm80_opt 1024 1024 1024
```

Expected:
```
BF16 GEMM (SM80 tensor cores, swizzle+LDSM): M=1024, N=1024, K=1024
Max error: <some value < 0.01>
PASS
Performance: <GFLOP/s> (<ms> ms per GEMM)
```

Must see PASS. If FAIL: debug the kernel — compare with sample 1 output.

- [ ] **Step 3: Run with additional sizes**

Run:
```bash
cd cute-reference/samples && ./bf16_gemm_sm80_opt 256 256 256
cd cute-reference/samples && ./bf16_gemm_sm80_opt 2048 2048 1024
```

Expected: Both PASS.

- [ ] **Step 4: Commit (if any fixes were needed)**

Only commit if compilation or runtime issues required fixes:
```bash
git add cute-reference/samples/bf16_gemm_sm80_opt.cu
git commit -m "Fix compilation/runtime issues in optimized BF16 GEMM sample"
```

---

### Task 7: Performance comparison

**Files:**
- Test: both `bf16_gemm_sm80` and `bf16_gemm_sm80_opt`

- [ ] **Step 1: Run both samples back-to-back at 1024x1024x1024**

Run:
```bash
cd cute-reference/samples && echo "=== Plain ===" && ./bf16_gemm_sm80 1024 1024 1024 && echo "" && echo "=== Optimized ===" && ./bf16_gemm_sm80_opt 1024 1024 1024
```

Record both GFLOP/s numbers. The optimized version should show a meaningful speedup from swizzle+LDSM eliminating bank conflicts and vectorizing smem loads.

- [ ] **Step 2: Run both at 2048x2048x1024 for peak throughput comparison**

Run:
```bash
cd cute-reference/samples && echo "=== Plain ===" && ./bf16_gemm_sm80 2048 2048 1024 && echo "" && echo "=== Optimized ===" && ./bf16_gemm_sm80_opt 2048 2048 1024
```

No commit needed for this task — it's purely measurement.
